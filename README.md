# Distributed-Log-Based-Database
A partitioned, Raft-based distributed log database with leader election, replication, snapshotting, custom RPC framework, consistent hashing, failure detection, and crash recovery.
